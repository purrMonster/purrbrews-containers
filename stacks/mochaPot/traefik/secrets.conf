# Cloudflare API token for DNS-01 certificates. "Edit zone DNS" template,
# scoped to the one zone. The same token works on every node.
CF_DNS_API_TOKEN  prompt Cloudflare API token (Zone → DNS → Edit, this zone only)
