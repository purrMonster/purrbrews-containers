[Unit]
Description=restic retention for the whole repository (forget --prune), Sundays at 03:00

[Timer]
# Sundays, after the store backup and before the Drive sync, so Drive gets the pruned repository.
OnCalendar=Sun *-*-* 03:00:00
Persistent=true

[Install]
WantedBy=timers.target
