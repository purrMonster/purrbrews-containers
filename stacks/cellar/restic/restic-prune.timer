[Unit]
Description=Run cellar's restic prune weekly

[Timer]
# Sunday 03:30: after the nightly 02:00 backup has finished, and off the
# nightly cadence so prune's I/O never stacks on top of it.
OnCalendar=Sun *-*-* 03:30:00
Persistent=true

[Install]
WantedBy=timers.target
