[Unit]
Description=Copy the backup repository to Google Drive, nightly at 03:30

[Timer]
# After the store backup (and Sunday's prune). The first run uploads everything.
OnCalendar=*-*-* 03:30:00
Persistent=true

[Install]
WantedBy=timers.target
