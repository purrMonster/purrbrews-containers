# Generated on this node — real domain and secrets. The tracked sources are
# local.env.example, the *.template files and generate-secrets.sh.
.env.local
*/secrets.env.local
# Rendered by render-configs.sh from */config/*.template
*/config/*
!*/config/*.template
