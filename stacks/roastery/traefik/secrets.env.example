# Copy to secrets.env.local and set your Cloudflare DNS API token.
# Loaded by start.ps1 into the process environment; never executed as code.
CF_DNS_API_TOKEN=REPLACE_ME
# Alternatively remove the token assignment above and use an absolute file path:
# CF_DNS_API_TOKEN_FILE=C:\private\cloudflare-token.txt
