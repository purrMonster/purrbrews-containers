# Laravel's APP_KEY: "base64:" and 32 random bytes, base64-encoded.
SPEEDTEST_TRACKER_APP_KEY         base64 32 base64:
# Replaces the image's built-in admin@example.com / password.
SPEEDTEST_TRACKER_ADMIN_PASSWORD  hex 32
