# Pinned rather than left to n8n: lose it and every stored credential in every
# workflow is unreadable. So it goes to flask too.
N8N_ENCRYPTION_KEY  hex 64
NOTE copy N8N_ENCRYPTION_KEY (n8n/secrets.env.local) to flask if you haven't yet
